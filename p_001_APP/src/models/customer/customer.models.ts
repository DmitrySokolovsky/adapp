export abstract class CustomerModel {
    public id: number;
    public name: string;
    public password: string;
}

export abstract class CustomerDataModel {
    public id: number;
    public name: string;
}